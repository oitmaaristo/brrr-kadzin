# Claudia sessioonilogi — 2026-03-19

## Mis tehti

### 1. Autoresearch agentide planeerimine ja uuring
- Risto tahab kahte autonoomset CC agenti (Karpathy autoresearch muster)
- **Stradivarius** — strateegia R&D loop (metaformer + uute strateegiate loomine), MNQ+MGC
- **Handel** — live tuning (filter_config.json + SL/TP), taisautonoomne piires
- Kolm uuringukatset (esimesed kaks jaid kinni, kolmas onnestus)
- Uuriti: CC hooks/settings.json tapse suuntaks, CLAUDE.md best practices, worktree isolation, autoresearch loop pattern

### 2. VPS koodi audit (printer2 tegelik struktuur)
- Vaatasin labi: src/strategies/ (30+ strateegiat), src/metaformer/ (Optuna+walk-forward), src/engine/, config/
- **KRIITILINE LEID:** Engine jookseb DASHBOARD SEES (src.dashboard.app --account 150K), MITTE eraldi protsess!
  - init_engine() -> TheEngineV2 -> asyncio.create_task(_start_engine_background())
  - Dashboard PID = Engine PID, port 8373
  - Heartbeat ja engine.log VOIVAD olla vanad eelmisest sessioonist
  - DataHub on eraldi protsess (src.data_hub.run)
- Handeli kontrollpind: data/filter_config.json (hot-reload JSON), bracket_calculations.py (SL=1.0xATR, TP=1.5xATR)
- Stradivariuse kontrollpind: src/strategies/, metaformer/run_optimization.py, strategy_factory.py, generate_strategies.py

### 3. WO-AGENTS-INFRA kirjutamine
- Kirjutasin taieliku WO CC-Windowsile (6 faasi)
- Sisaldab: settings.json molema agendi jaoks, CLAUDE.md-d, launch scriptid, health check, dry run juhised
- Max plan = no --max-budget-usd, ainult --max-turns konteksti varskendamiseks

### 4. CC-Windows implementeeris WO-AGENTS-INFRA
- Koik faasid 1-5 labi (worktree'd, settings, CLAUDE.md-d, scriptid, shared state)
- Stradivarius dry run: PASS (luges faile, jooksis loopi, 10 turni)
- Handel dry run: PASS (vaatles, paring trading.db, sleep 60 kui no triggers, 3 iteratsiooni logitud)
- Claudia review: PASS koik kontrollid

## Otsused
- Agendid kasutavad sibling worktree'd (mitte .claude/worktrees/) — valtimaks CC bugs #28248, #29599
- Stop hook blokeerib agendi valjumise (Ralph pattern), stop_hook_active check valtimaks infinite loop
- Handel deny list: engine.py, engine_wiring.py, order_executor.py, websocket_manager.py, CRM, metaformer
- Stradivarius deny list: kogu src/engine/, src/crm/, src/api/

## Vead ja opitud
- **ENGINE VIGA:** Claudia jareldas et engine ei jookse (vana heartbeat Mar 13, vana engine.log). Tegelikult engine jookseb dashboard sees! Dashboard screenshot naitad Engine rohelise tule — Claudia oleks pidanud koodi vaatama, mitte arvama.
- **Reegel:** Enne jarelduste tegemist AVA KOOD ja vaata kuidas asjad tegelikult tootavad. ps aux ei naitagi engine protsessi eraldi, sest see on dashboard sees.

## Pooleli / TODO
- Phase 6 production launch ootab Risto  go (molemad agendid)
- Hook logging artefakt: SQL paringu * laieneb shellis (failinimed SELECT-is) — ainult logi probleem, tegelik paring tootab

## Failid
- WO-AGENTS-INFRA.md — taielik juhend CC-Windowsile
- /home/brrr/brrr-stradivarius/ — worktree + CLAUDE.md + settings.json
- /home/brrr/brrr-handel/ — worktree + CLAUDE.md + settings.json
- /home/brrr/agent-scripts/ — 5 scripti (launch x2, loop x2, health)
- /home/brrr/agent-logs/ — logikaust
- /var/log/claude-agents/ — hook logid

## VPS protsessid (2026-03-19 seisuga)
- Dashboard+Engine: PID 2988256, venv/bin/python -m src.dashboard.app --account 150K (port 8373)
- DataHub: PID 2007059, venv/bin/python -m src.data_hub.run (alates Mar 17)
- Hankeradar: PID 1825883, streamlit (port 8501)
- Flux: bun, port 3000
- Autoradar: Docker (frontend 3010, backend 8001, postgres 5433)
